# Rename to .gitignore in the repo.

# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/

# --- OS / editors ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# NOTE: state and track-record files (subgate_state.json, subgate_full.txt,
# subgate_chrome.txt) are intentionally NOT ignored here. The GitHub Actions
# workflow commits them so the catalog and published lists persist across
# runs, and git history is their backup. They are kept out of handback zips
# via the manifest in HANDOFF.md, not via this file. Do not add them to this
# list.
